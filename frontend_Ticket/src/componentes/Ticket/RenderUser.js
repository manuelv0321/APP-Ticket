import React from 'react';
import { Link } from 'react-router-dom';
import Swal from 'sweetalert2';
import clienteAxios from '../../config/axios';

function RenderUser({ cliente }) {

    console.log(cliente);

}
export default RenderUser;
